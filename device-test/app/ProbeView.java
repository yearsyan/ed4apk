package dev.aqe.smoketest;

import android.content.Context;
import android.util.AttributeSet;
import android.widget.TextView;

/** Loaded by name from binary layout XML rather than a typed DEX reference. */
public class ProbeView extends TextView {
    public ProbeView(Context context, AttributeSet attributes) { super(context, attributes); }
}
